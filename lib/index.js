/**
 * dsh-settings-width — server-side stub.
 * All real logic lives in the browser (lib/client.js); the empty `apply`
 * just lets the cordis.patch.yml loader entry exist (a row without an
 * apply would crash the profile boot scan).
 */
export function apply() {}
